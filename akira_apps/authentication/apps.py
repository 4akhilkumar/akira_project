from django.apps import AppConfig


class AuthenticationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'akira_apps.authentication' # Add path i.e., akira_apps in name
