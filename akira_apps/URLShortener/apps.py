from django.apps import AppConfig


class UrlshortenerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'akira_apps.URLShortener' # Add path i.e., akira_apps in name
